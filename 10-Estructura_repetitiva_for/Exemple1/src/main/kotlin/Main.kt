/**
 * For a Kotlin te algunes variants, en aquest cas veurem for amb expresions de rang.
 * Un rang defineix un interval amb un valor inicial i un vlor final. i es defineix utilitzan el valor ..
 * Exemples de definició de rang serien:
 * val undigit = 1..9
 * val dotzena = 1..12
 * var lletres = 'a'..'z'
 *
 * Si nesecitem coneixer si un valor es troba dins un rang hem d'utilitzar l'operador in o !in
 *
 *
 */

fun main(args: Array<String>) {
    println("Hello World!")

    // Try adding program arguments via Run/Debug configuration.
    // Learn more about running applications: https://www.jetbrains.com/help/idea/running-applications.html.
    println("Program arguments: ${args.joinToString()}")
}