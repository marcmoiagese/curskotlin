# Estructura condicional when amb argument

Amb when tambe podem pasarli un argument despres de la paraula when com si fos el case aqui tens un exemple per a que quedi mes clar

```kotlin
when (valor) {
    1 -> print("hola")
    2 -> print("Adeu")
}
```
