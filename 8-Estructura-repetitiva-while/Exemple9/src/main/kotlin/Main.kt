/**
 * Imprimeix per pantalla multiples de 8 fins a 500, tens que mostrar per pantalla 8, 16, 24, etc
 */
fun main(args: Array<String>) {
    var vuitM =  8

    while(vuitM <= 500){
        println(vuitM)
        vuitM += 8
    }
}